planned_tasks = ["vacuum clean", "do the laundry", "mow the lawn"]
print(planned_tasks)

# List for planned tasks, because the list can change.

birth_of_date = (9, 9, 1990)
print(birth_of_date)

# Tuple for birth of dates, because the data not need to change.